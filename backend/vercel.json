{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node",
      "config": { "typescript": true }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/index.ts" },
    { "src": "/(.*)", "dest": "src/index.ts" }
  ]
}